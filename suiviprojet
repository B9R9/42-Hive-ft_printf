Projet visant a recoder la recoder la fucntion printf

Prototype:
	int	printf (const char *restrict format, ...)

printf:
	 format and print data
Description:
	Print argument(s) according to Format, or execute according to option
	
	--help display man and exit
	--version output version information and exit
	ecrit sur le STDOUT

Valeur renvoye:
	Si elle reussit, cette fucntion renvoie le nombre de caracteres imprimes,
sans l octel \0 final dans les chaines

FONCTION VARIADIQUE:

#include <stdarg.h>

void print(consr char *str, ...)
{
	char *containeur;
	va_list ap; //recuere les arguments
	va_start(ap, str);inialise le debut des arguments variables avec le dernier
argument connus
	containeur = va_arg(ap, char *)//permet dacceder a  l argument en precisant
le type de la variable
	printf("%s\n",str);
	printf("%s\n",containeur);
	containeur = va_arg(ap, char *)//acces au 2 deuxieme arguement

	va_end(ap); libere la macro
}

Problematique:
	-> Allocation memoire:
		str sera toujours plus pe


18/02
->	makefile done

25/02
-> flag u done

28/02
-> all flag dispo -> done

04/03
quelques chose de fonctionnel avec le flag s
probleme avec reptition des flags avec option
enc as derror, seulement l error devrait s afficher et pas la str


Precision 0:
%050
	indique la longueur de la str a imprime, si %n < lenght str  rien ne se
	passe. Si %n > complette le reste avex des zero.
%12 
	indique la longeur de la str imprime. si %12 < lentgh(str) rien ne se
	passe. Si %12>  complete la diff par des espaces

Precision space
% d
	insert un espace avant la valeur



-> creer une struture pour les parametre des option et une function qui va
stocker les infos dans la struture
-> faire les precisions
-> verifier les  les dirrents scenarios
-> creer un fichier test
-? verifier les types assigne
-> mettre a la NORM
-> tester les extremes de valeurs genre max int
 
